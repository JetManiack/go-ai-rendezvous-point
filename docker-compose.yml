# Local Postgres for testing internal/storage's Postgres backend. Bring it
# up, point TEST_POSTGRES_DSN at it, and every existing internal/storage
# test runs against Postgres too (via the shared openTestDB helper), not
# just the Postgres-specific tests:
#
#   docker compose up -d
#   TEST_POSTGRES_DSN=postgres://rendezvous:rendezvous@localhost:5432/rendezvous_test go test ./internal/storage/... -v
#   docker compose down
#
# Without TEST_POSTGRES_DSN set, Postgres-only tests report SKIP and the
# rest of the suite runs against SQLite as usual — this container is never
# required for a normal `go test ./...`.
services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: rendezvous
      POSTGRES_PASSWORD: rendezvous
      POSTGRES_DB: rendezvous_test
    ports:
      - "5432:5432"
